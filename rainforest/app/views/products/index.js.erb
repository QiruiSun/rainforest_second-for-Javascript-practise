<% if params[:search] && parmas[:page].nil? %>
  $('#products').html("<%= j(render @products) %>");
<% else %>
  $('#products').append("<%= j render @products %>");
<% end %>

$('.pagination').replaceWith("<%= j paginate(@products) %>");
